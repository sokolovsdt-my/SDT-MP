-- Мёртвая таблица: в текущем коде на main и во всех живых ветках —
-- ноль ссылок (вся работа со слотами идёт через teacher_slot_dates).
-- В проде содержит 2 строки, никем не читается, никем не пишется.
-- Старая работа в брошенной ветке epic-dhawan, которая использовала её
-- через файл AdminIndivs.jsx, удалена (ветка дропнута, файл на main
-- никогда не существовал).
drop table if exists public.teacher_indiv_slots;
